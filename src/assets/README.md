# Source Assets

This folder is intentionally kept empty for now.

Approved runtime PNGs should be placed under `public/assets` using the paths defined in `src/data/CharacterData.ts` and `src/data/GameAssetData.ts`.
